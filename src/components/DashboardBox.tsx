import { Box } from "@mui/material";
import { styled } from "@mui/system";

const DashboardBox = styled(Box)(({ theme }) => ({
  backgroundColor: " #1A1A1D;",
  borderRadius: "2rem",
  boxShadow: "0.15rem 0.2rem 0.15rem 0.1rem #346073;",
}));

export default DashboardBox;
